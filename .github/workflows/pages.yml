---
# Builds the training site and publishes it to GitHub Pages.
#
# The repository Pages setting is **Source: GitHub Actions**, so this uploads an
# artifact and deploys it directly. There is no gh-pages branch and nothing is
# force-pushed anywhere.
name: Build and publish the training site

on:
  push:
    branches:
      - main
      # TEMPORARY, remove when this branch merges: workflow_dispatch only becomes
      # available once the workflow exists on the default branch, so this is the
      # only way to publish and check the site before the merge.
      - feat/training-v1
  workflow_dispatch:

permissions: read-all

# One deployment at a time, and never cancel one half way through
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build with Zensical
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          persist-credentials: false
          fetch-depth: 0

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24

      # Everything the site publishes is generated from training-universe.json and
      # the lab front matter, so a drifted artifact fails here rather than reaching
      # a learner.
      - name: Regenerate the derived files
        run: |
          node scripts/build/universe.mjs --check
          node scripts/build/site.mjs

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: 3.x

      # Zensical is the successor of mkdocs-material and reads the same mkdocs.yml
      - run: pip install zensical mdx_truly_sane_lists
      - run: python -m zensical build

      - name: Every page resolves every asset it references
        run: node scripts/verify/check-site.mjs

      - uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3.0.1
        with:
          path: site

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4.0.5
